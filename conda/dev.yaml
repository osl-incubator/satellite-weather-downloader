name: env-satellite
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python >=3.10
  - pip
  - docker-compose
  - celery
  - make
  - pip:
    - git+https://github.com/luabida/satellite-weather-downloader.git@xarray-refactor #TODO: remove it when bumping version
    # - satellite-weather-downloader
